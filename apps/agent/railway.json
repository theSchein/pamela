{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/agent && npm install && npm run build"
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 1,
    "overlapSeconds": 0,
    "drainingSeconds": 0,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "cd apps/agent && npm start"
  }
}